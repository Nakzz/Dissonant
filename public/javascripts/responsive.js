jQuery(document).ready(function ($) {
            $.noConflict();
var height = window.innerHeight;
var padding = height * .3;
$(".construction").css('min-height', (height));
$(".construction").css('padding-top', (padding));

// $(".construction").css('margin-top', ("-100vh"));


});